%main.cd-main
	%section.cd-section.index.visible
		/ content-block starts here
		.content-block.light
			.container
				=render 'shared/top_bar'
				#home.content
					.flexslider.flex-slider
						%ul#projects.projects.slides
							-@projects.each do |project|
								%li
									.image.grayscale
										=image_tag project.projectphotos.first.image.url(:hot_image)
									.right
										.middle
											.title
												=link_to project.title
										.helper
									.left
										.middle
											.text-short
												%p
													%strong
														=truncate(project.desctitle, length: 50)
												%p
													=truncate(project.description, length: 250)
											.read-more
												=link_to 'Подробнее', front_project_path(project.id)
										.helper